# Second-Order Winner
After a first-layer expansion, ask which layer becomes the next constraint.
Example migration: GPU → packaging → equipment → component → power → cooling → grid → permitting.
Bind to Bottleneck Migration. First-order names are often already priced.
Sidecar only. No live score. No forced ticker.
